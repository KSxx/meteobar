{
  "schemaVersion": 1,
  "id": "mryll.meteobar",
  "name": "Meteobar",
  "version": "1.0.0",
  "author": "mryll",
  "license": "MIT",
  "description": "Open-Meteo weather (meteobar) in the Omarchy bar: condition glyph + temperature, with an hourly and daily forecast panel.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "omarchy/BarWidget.qml"
  },
  "barWidget": {
    "displayName": "Meteobar",
    "description": "Click: forecast panel. Middle click: refresh.",
    "category": "Status",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "refreshMinutes": 15,
      "units": "metric",
      "location": "",
      "iconSet": "nerd",
      "colorMode": "full"
    },
    "schema": [
      {
        "key": "refreshMinutes",
        "type": "integer",
        "label": "Refresh interval (minutes)",
        "min": 1,
        "max": 180,
        "step": 1,
        "defaultValue": 15
      },
      {
        "key": "units",
        "type": "enum",
        "label": "Units",
        "options": [
          "metric",
          "imperial"
        ],
        "defaultValue": "metric"
      },
      {
        "key": "location",
        "type": "string",
        "label": "Location",
        "defaultValue": "",
        "description": "City name, 'City, Province' or 'City, CC' (meteobar's geocoding disambiguation). Empty auto-detects by IP."
      },
      {
        "key": "iconSet",
        "type": "enum",
        "label": "Icon set",
        "options": [
          "nerd",
          "weather",
          "emoji",
          "fontawesome"
        ],
        "defaultValue": "nerd",
        "description": "Glyph family for condition icons. 'fontawesome' requires otf-font-awesome >= 7."
      },
      {
        "key": "colorMode",
        "type": "enum",
        "label": "Colors",
        "options": ["full", "none", "bar-only", "panel-only"],
        "defaultValue": "full",
        "description": "Monochrome rendering uses foreground tones only. 'none' drops color everywhere, 'bar-only' keeps it on the bar face, 'panel-only' keeps it in the panel."
      }
    ]
  }
}
